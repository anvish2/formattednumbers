{
  "name": "formattednumbers",
  "displayName": "Formatted Numbers",
  "version": "0.0.1",
  "description": "Automatically render thousands separators",
  "publisher": "anvish",
  "repository": {
    "type": "git",
    "url": "https://github.com/anvish2/formattednumbers"
  },
  "main": "./extension.js",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "devDependencies": {
    "@types/vscode": "^1.74.0"
  },
  "files": [
    "extension.js",
    "package.json",
    "LICENSE",
    "readme.md"
  ],
  "contributes": {
    "configuration": {
      "properties": {
        "formattedNumbers.style": {
          "order": 1,
          "type": "string",
          "default": "underline",
          "enum": [
            "underline",
            "inline"
          ],
          "description": "The style of the thousands separator.",
          "enumDescriptions": [
            "A subtle separator below the line. This keeps line width intact.",
            "A standard greyed out comma. This makes line longer."
          ]
        },
        "formattedNumbers.minLength": {
          "order": 2,
          "type": "number",
          "default": 5,
          "description": "The minimum length of a number to be formatted. Recommended is 5, i.e. starting with 10,000, this will keep 4-digit years without separator."
        },
        "formattedNumbers.maxLength": {
          "order": 3,
          "type": "number",
          "default": 500,
          "description": "The maximum length of a number to be formatted."
        },
        "formattedNumbers.throttle": {
          "order": 4,
          "type": "number",
          "default": 300,
          "description": "The delay in milliseconds before updating decorations. Set to 0 for immediate updates."
        }
      }
    }
  }
}
